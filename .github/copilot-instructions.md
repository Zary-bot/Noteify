# Noteify workspace

- Native Kotlin Android app using Jetpack Compose and Material 3.
- Keep the app offline-first. Do not add network dependencies or remote persistence.
- User deadlines and reminder preferences must remain local to the device.
- Prefer focused changes and verify with `gradlew.bat assembleDebug` on Windows.
